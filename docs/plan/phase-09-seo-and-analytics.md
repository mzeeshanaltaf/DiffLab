# Phase 09 — SEO and analytics

**Goal** — discoverability. Per-route metadata, `sitemap.ts`, `robots.ts` (with `/d/*` noindexed),
`opengraph-image.tsx` via `next/og`, JSON-LD (`SoftwareApplication` + `FAQPage`), canonical URLs on
`https://difflab.zeeshanai.cloud`. Then **run the `seo-audit` skill** and fix what it reports.
`@vercel/analytics` via the `add-vercel-analytics` skill.
