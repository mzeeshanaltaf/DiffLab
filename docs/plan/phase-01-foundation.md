# Phase 01 — Foundation

**Goal** — a running, styled, empty shell.

`create-next-app` (TS, Tailwind v4, App Router, ESLint) → `shadcn` init → design tokens matching the
dark/emerald direction → fonts → `site-header.tsx` (logo, mode tabs, theme toggle) → `site-footer.tsx`
containing **`Developed with 💖 by [Zeeshan Altaf](https://zeeshanai.cloud)`** → theme provider with
dark default → `docs/plan/` populated.

**Done when** `npm run dev` boots, both themes render, and the footer credit links out correctly.
