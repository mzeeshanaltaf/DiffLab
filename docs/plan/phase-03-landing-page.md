# Phase 03 — Landing page

**Goal** — the bold marketing surface at `/`.

Hero with an animated live-diff demo, feature grid, use-case sections (developers / writers / ops),
file-type support, FAQ, CTA. Uses `high-end-visual-design` + `framer-motion-animator`. Respect
`prefers-reduced-motion`.

**Done when** Lighthouse Performance ≥ 90 on `/` despite the motion work.
